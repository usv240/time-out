# Secrets — never commit
.env
.env.local
*.key
*.pem

# Cached API responses (regenerate; may contain sponsor quota data)
.cache/
fixtures/live/

# Deps & builds
node_modules/
dist/
build/
__pycache__/
*.pyc
.venv/
venv/

# Editor / OS
.vscode/
.DS_Store
Thumbs.db

# Media working files (demo renders are large)
*.mp4
*.mov
!demo/final/*.mp4

# Scraped sponsor page — contains shared credentials, keep local only
Rules.md
OUTREACH.md

# Strategy & planning — keep local. Repo goes public for the Nutrient challenge;
# competitors should not get our audit, prize tactics, or demo script.
OUTREACH.md
research/teach-back-competitive-audit.md
submission/video-script.md

plan/

# Live API response cache (regenerate with the live clients)
.cache/

# Sponsor-issued Postman collection — contains a demo API key
doctavian-demo.postman_collection.json
OUTREACH-*.md
